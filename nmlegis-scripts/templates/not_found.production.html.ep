% layout 'default';
% title 'Not Found';

% # FIXME: try smart-parsing the URL and offering suggestions
% my $url = $self->req->url->path;
% use Data::Dump; dd "NOT FOUND", $url;
% $self->app->log->warn("404 not found: $url");  # FIXME: referrer?

<h1>Not Found</h1>

<h3>Try a search?</h3>
%= form_for '/search' => begin
<div id="bigsearch">
  %= text_field 'q', size => 30
</div>
%= end

% $self->stash('skip_legend' => 1);
